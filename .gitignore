# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/migrations/dev.db

# editor
.vscode/*
!.vscode/extensions.json

# claude code (machine-local, syncs to Obsidian via OneDrive)
.claude/

# notas de sessão do Claude Code (contexto local, não versionado)
CONTEXTO-SESSAO-*.md

# export de devices do NetBox com IPs internos e números de série — dado sensível,
# não deve ir pro histórico do git. Copiar manualmente (scp) para onde o import for rodar.
netbox_cameras.csv

# imagens de fundo enviadas pela UI (fotos de drone/planta) — arquivo binário grande,
# específico de cada ambiente, não deve ir pro git.
/public/uploads/
