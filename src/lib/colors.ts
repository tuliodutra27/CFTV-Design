// Cor default do cone de alcance das câmeras ativas — usada como fallback tanto no frontend
// (CctvCanvas, antes de carregar a preferência salva) quanto na API de settings (quando ninguém
// nunca configurou nem uma cor pessoal nem a cor global).
export const DEFAULT_RANGE_COLOR = '#22c55e';
